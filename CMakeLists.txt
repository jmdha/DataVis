# This file is automatically generated from cmake.toml - DO NOT EDIT
# See https://github.com/build-cpp/cmkr for more information

cmake_minimum_required(VERSION 3.15)

if(CMAKE_SOURCE_DIR STREQUAL CMAKE_BINARY_DIR)
	message(FATAL_ERROR "In-tree builds are not supported. Run CMake from a separate directory: cmake -B build")
endif()

# Regenerate CMakeLists.txt automatically in the root project
set(CMKR_ROOT_PROJECT OFF)
if(CMAKE_CURRENT_SOURCE_DIR STREQUAL CMAKE_SOURCE_DIR)
	set(CMKR_ROOT_PROJECT ON)

	# Bootstrap cmkr
	include("cmake/cmkr.cmake" OPTIONAL RESULT_VARIABLE CMKR_INCLUDE_RESULT)
	if(CMKR_INCLUDE_RESULT)
		cmkr()
	endif()

	# Enable folder support
	set_property(GLOBAL PROPERTY USE_FOLDERS ON)
endif()

# Create a configure-time dependency on cmake.toml to improve IDE support
if(CMKR_ROOT_PROJECT)
	configure_file(cmake.toml cmake.toml COPYONLY)
endif()

project(DataVis)

if(CMKR_ROOT_PROJECT AND NOT CMKR_DISABLE_VCPKG)
	include(FetchContent)
	message(STATUS "Fetching vcpkg...")
	FetchContent_Declare(vcpkg URL "https://github.com/microsoft/vcpkg/archive/refs/tags/2021.05.12.tar.gz")
	FetchContent_MakeAvailable(vcpkg)
	include("${vcpkg_SOURCE_DIR}/scripts/buildsystems/vcpkg.cmake")
endif()

# Packages
find_package(glfw3 REQUIRED)

find_package(GLEW REQUIRED)

# Target imgui
set(CMKR_TARGET imgui)
set(imgui_SOURCES "")

list(APPEND imgui_SOURCES
	"third_party/imgui/imgui.cpp"
	"third_party/imgui/imgui_demo.cpp"
	"third_party/imgui/imgui_draw.cpp"
	"third_party/imgui/imgui_tables.cpp"
	"third_party/imgui/imgui_widgets.cpp"
	"third_party/imgui/imconfig.h"
	"third_party/imgui/imgui.h"
	"third_party/imgui/imgui_internal.h"
	"third_party/imgui/imstb_rectpack.h"
	"third_party/imgui/imstb_textedit.h"
	"third_party/imgui/imstb_truetype.h"
	"third_party/imgui/backends/imgui_impl_glfw.cpp"
	"third_party/imgui/backends/imgui_impl_glfw.h"
	"third_party/imgui/backends/imgui_impl_opengl3.cpp"
	"third_party/imgui/backends/imgui_impl_opengl3.h"
)

list(APPEND imgui_SOURCES
	cmake.toml
)

set(CMKR_SOURCES ${imgui_SOURCES})
add_library(imgui STATIC)

if(imgui_SOURCES)
	target_sources(imgui PRIVATE ${imgui_SOURCES})
endif()

source_group(TREE ${CMAKE_CURRENT_SOURCE_DIR} FILES ${imgui_SOURCES})

target_compile_features(imgui PUBLIC
	cxx_std_20
)

target_include_directories(imgui PUBLIC
	"third_party/imgui"
)

target_link_libraries(imgui PUBLIC
	glfw
	GLEW::GLEW
)

unset(CMKR_TARGET)
unset(CMKR_SOURCES)

# Target implot
set(CMKR_TARGET implot)
set(implot_SOURCES "")

list(APPEND implot_SOURCES
	"third_party/implot/implot.cpp"
	"third_party/implot/implot_demo.cpp"
	"third_party/implot/implot_items.cpp"
	"third_party/implot/implot.h"
	"third_party/implot/implot_internal.h"
)

list(APPEND implot_SOURCES
	cmake.toml
)

set(CMKR_SOURCES ${implot_SOURCES})
add_library(implot STATIC)

if(implot_SOURCES)
	target_sources(implot PRIVATE ${implot_SOURCES})
endif()

source_group(TREE ${CMAKE_CURRENT_SOURCE_DIR} FILES ${implot_SOURCES})

target_include_directories(implot PUBLIC
	"third_party/imgui"
)

unset(CMKR_TARGET)
unset(CMKR_SOURCES)

# Target DataVis
set(CMKR_TARGET DataVis)
set(DataVis_SOURCES "")

list(APPEND DataVis_SOURCES
	"src/main.cpp"
	"src/IO/GUI/GUIManager.cpp"
	"third_party/ImGuiFileDialog/ImGuiFileDialog.cpp"
	"src/IO/GUI/GUIManager.h"
	"src/IO/GUI/GUIElements/ClassifierViewer.h"
	"src/IO/GUI/GUIElements/DataViewer.h"
	"src/IO/GUI/GUIElements/FeatureViewer.h"
	"src/IO/GUI/GUIElements/GUIElement.h"
	"src/misc/Constants.h"
	"src/misc/DataManager.h"
	"src/utilities/ClassCountUtils.h"
	"src/utilities/FeatureUtils.h"
	"src/utilities/SeriesUtils.h"
	"src/types/Feature.h"
	"src/core/feature_finding/Evaluation.h"
	"src/core/feature_finding/FeatureFinding.h"
	"src/core/attributes/Attribute.h"
	"src/core/attributes/AttributeBuilder.h"
	"src/core/attributes/Frequency.h"
	"src/core/attributes/MaxDist.h"
	"src/core/attributes/MinDist.h"
	"src/core/attributes/OccPos.h"
	"third_party/ImGuiFileDialog/ImGuiFileDialog.h"
	"third_party/ImGuiFileDialog/ImGuiFileDialogConfig.h"
)

list(APPEND DataVis_SOURCES
	cmake.toml
)

set(CMKR_SOURCES ${DataVis_SOURCES})
add_executable(DataVis)

if(DataVis_SOURCES)
	target_sources(DataVis PRIVATE ${DataVis_SOURCES})
endif()

get_directory_property(CMKR_VS_STARTUP_PROJECT DIRECTORY ${PROJECT_SOURCE_DIR} DEFINITION VS_STARTUP_PROJECT)
if(NOT CMKR_VS_STARTUP_PROJECT)
	set_property(DIRECTORY ${PROJECT_SOURCE_DIR} PROPERTY VS_STARTUP_PROJECT DataVis)
endif()

source_group(TREE ${CMAKE_CURRENT_SOURCE_DIR} FILES ${DataVis_SOURCES})

target_compile_features(DataVis PRIVATE
	cxx_std_20
)

target_include_directories(DataVis PRIVATE
	src
	third_party
)

target_link_libraries(DataVis PRIVATE
	imgui
	implot
)

unset(CMKR_TARGET)
unset(CMKR_SOURCES)

